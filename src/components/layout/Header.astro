---
import { Image } from "astro:assets";

import logoBlack from "../../assets/images/brand/GLOPS2.png";
import { site } from "../../config/site";
import { withBase } from "../../lib/paths";

interface Props {
  /** Remate bajo la barra: damero en la portada, doble filete en la carta. */
  strip?: "checker" | "rule";
  /**
   * Presentación compacta para la home: solo el logo gráfico, sin repetir el nombre,
   * que ya lleva el H1 del hero. En páginas interiores el nombre acompaña al logo.
   */
  compact?: boolean;
}

const { strip = "checker", compact = false } = Astro.props;
---

<header class:list={["site-header", { "site-header--compact": compact }]} data-site-header>
  <div class="bar">
    <a class="brand" href={withBase("/")}>
      <Image
        src={logoBlack}
        alt={`${site.name}, inicio`}
        widths={[150]}
        sizes="44px"
        loading="eager"
      />
      {!compact && <span class="brand__name">Glops<span aria-hidden="true"> i </span>Llandes</span>}
    </a>

    <nav class="nav-desktop" aria-label="Navegación principal">
      <ul>
        {
          site.nav.map((item) => (
            <li>
              <a href={item.href}>{item.label}</a>
            </li>
          ))
        }
      </ul>
    </nav>

    <div class="actions">
      <a class="phone" href={site.phone.href}>{site.phone.display}</a>
      <button
        class="burger"
        type="button"
        aria-expanded="false"
        aria-controls="menu-movil"
        data-menu-toggle
      >
        <span class="burger__lines" aria-hidden="true">
          <span class="burger__line"></span>
          <span class="burger__line"></span>
          <span class="burger__line"></span>
        </span>
        <span class="visually-hidden" data-menu-label>Abrir menú</span>
      </button>
    </div>
  </div>

  <!-- Velo sobre la página mientras el menú está abierto; tocarlo cuenta como «fuera». -->
  <div class="scrim" hidden data-menu-scrim aria-hidden="true"></div>

  <div class="panel" id="menu-movil" hidden data-menu-panel>
    <nav aria-label="Navegación móvil">
      <ul>
        {
          site.nav.map((item) => (
            <li>
              <a href={item.href} data-menu-link>
                {item.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
    <a class="btn btn--brand panel__cta" href={site.phone.href}>
      Llamar {site.phone.display}
    </a>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 20;
    background: var(--color-paper);
  }

  .bar {
    /* Por encima del velo y del panel, para que el botón siga accesible con el menú abierto. */
    position: relative;
    z-index: 2;
    display: flex;
    align-items: center;
    gap: var(--space-m);
    width: 100%;
    max-width: var(--container);
    min-height: var(--header-height);
    margin-inline: auto;
    padding: 0.5rem var(--gutter);
    background: var(--color-paper);
  }

  .brand {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    margin-right: auto;
    text-decoration: none;
  }

  .brand :global(img) {
    width: 44px;
    height: auto;
  }

  .brand__name {
    font-family: var(--font-display);
    font-size: 1.05rem;
    font-weight: 700;
    letter-spacing: 0.02em;
    line-height: 1;
    text-transform: uppercase;
  }

  /* Home: solo el logo, algo más contenido. */
  .site-header--compact .bar {
    padding-block: 0.35rem;
  }

  .site-header--compact .brand :global(img) {
    width: 40px;
  }

  .nav-desktop {
    display: none;
  }

  .nav-desktop ul {
    display: flex;
    gap: var(--space-m);
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .nav-desktop a {
    display: inline-block;
    padding: 0.35rem 0.5rem;
    border-bottom: 2px solid transparent;
    font-family: var(--font-mono);
    font-size: var(--text-s);
    letter-spacing: 0.1em;
    text-decoration: none;
    text-transform: uppercase;
  }

  /* Inversión: el mostaza como texto sobre papel no llega al contraste mínimo. */
  .nav-desktop a:hover {
    border-bottom-color: var(--color-black);
    background: var(--color-brand);
    color: var(--color-black);
  }

  .actions {
    display: flex;
    align-items: center;
    gap: var(--space-s);
  }

  .phone {
    display: none;
    padding: 0.3rem 0.5rem;
    border-radius: var(--radius-s);
    font-family: var(--font-mono);
    font-size: var(--text-s);
    letter-spacing: 0.06em;
    text-decoration: none;
  }

  .phone:hover {
    background: var(--color-brand);
    color: var(--color-black);
  }

  .burger {
    display: grid;
    place-items: center;
    width: 3rem;
    height: 3rem;
    padding: 0;
    border: 2px solid var(--color-black);
    border-radius: var(--radius-m);
    background: var(--color-paper);
    cursor: pointer;
  }

  /* Tres líneas que se convierten en cruz: la del medio desaparece y las otras giran. */
  .burger__lines {
    position: relative;
    display: block;
    width: 20px;
    height: 14px;
  }

  .burger__line {
    position: absolute;
    left: 0;
    display: block;
    width: 100%;
    height: 2px;
    background: var(--color-black);
    transition:
      translate 0.2s var(--ease-out),
      rotate 0.2s var(--ease-out),
      opacity 0.15s linear;
  }

  .burger__line:nth-child(1) {
    top: 0;
  }

  .burger__line:nth-child(2) {
    top: 6px;
  }

  .burger__line:nth-child(3) {
    top: 12px;
  }

  .burger[aria-expanded="true"] {
    background: var(--color-brand);
    border-color: var(--color-brand);
  }

  .burger[aria-expanded="true"] .burger__line:nth-child(1) {
    translate: 0 6px;
    rotate: 45deg;
  }

  .burger[aria-expanded="true"] .burger__line:nth-child(2) {
    opacity: 0;
  }

  .burger[aria-expanded="true"] .burger__line:nth-child(3) {
    translate: 0 -6px;
    rotate: -45deg;
  }

  .strip.checker {
    --checker-size: 10px;
    height: 10px;
  }

  .strip--rule {
    height: 5px;
    border-top: 1px solid var(--color-black);
    border-bottom: 1px solid var(--color-black);
  }

  /* Oscurece la página (y el vídeo del hero) mientras el menú está abierto. */
  .scrim {
    position: fixed;
    inset: 0;
    z-index: 0;
    background: rgb(10 10 10 / 0.6);
  }

  /*
   * El panel se superpone bajo la barra en vez de empujar la página. Su altura máxima
   * se ajusta a la pantalla real del móvil y, si no cabe, hace scroll interno.
   */
  .panel {
    position: absolute;
    top: 100%;
    left: 0;
    right: 0;
    z-index: 1;
    max-height: calc(100vh - var(--header-height));
    max-height: calc(100svh - var(--header-height));
    overflow-y: auto;
    overscroll-behavior: contain;
    border-bottom: var(--frame-width) solid var(--color-brand);
    background: var(--color-black);
    color: var(--color-paper);
    padding: var(--space-m) var(--gutter) var(--space-l);
  }

  .scrim[hidden],
  .panel[hidden] {
    display: none;
  }

  .panel ul {
    display: grid;
    gap: 0.25rem;
    margin: 0 0 var(--space-m);
    padding: 0;
    list-style: none;
  }

  .panel a[data-menu-link] {
    display: block;
    padding: 0.85rem 0;
    border-bottom: 1px solid #2a2a2a;
    color: var(--color-paper);
    font-family: var(--font-display);
    font-size: var(--title-s);
    font-weight: 700;
    letter-spacing: 0.02em;
    text-decoration: none;
    text-transform: uppercase;
  }

  .panel a[data-menu-link]:hover,
  .panel a[data-menu-link]:focus-visible {
    color: var(--color-brand);
  }

  .panel__cta {
    width: 100%;
  }

  @media (min-width: 62rem) {
    .nav-desktop,
    .phone {
      display: block;
    }

    .burger,
    .scrim,
    .panel {
      display: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .burger__line {
      transition: none;
    }
  }
</style>

<script>
  const header = document.querySelector<HTMLElement>("[data-site-header]");

  // Si el script volviera a ejecutarse, el header ya inicializado no repite listeners.
  if (header && header.dataset.menuReady !== "true") {
    header.dataset.menuReady = "true";

    const toggle = header.querySelector<HTMLButtonElement>("[data-menu-toggle]");
    const panel = header.querySelector<HTMLElement>("[data-menu-panel]");
    const scrim = header.querySelector<HTMLElement>("[data-menu-scrim]");
    const label = toggle?.querySelector<HTMLElement>("[data-menu-label]");

    if (toggle && panel) {
      const isOpen = () => toggle.getAttribute("aria-expanded") === "true";

      const setOpen = (open: boolean) => {
        toggle.setAttribute("aria-expanded", String(open));
        panel.hidden = !open;
        if (scrim) scrim.hidden = !open;
        if (label) label.textContent = open ? "Cerrar menú" : "Abrir menú";
      };

      // Hamburguesa / cruz.
      toggle.addEventListener("click", () => setOpen(!isOpen()));

      // Dentro del panel solo cierra un enlace (navegación o teléfono), no el resto del área.
      panel.addEventListener("click", (event) => {
        if ((event.target as Element).closest("a")) setOpen(false);
      });

      // Clic o toque fuera del panel y del botón (incluido el velo).
      document.addEventListener("pointerdown", (event) => {
        if (!isOpen()) return;
        const target = event.target as Node;
        if (panel.contains(target) || toggle.contains(target)) return;
        setOpen(false);
      });

      // Escape cierra y devuelve el foco al botón.
      document.addEventListener("keydown", (event) => {
        if (event.key !== "Escape" || !isOpen()) return;
        setOpen(false);
        toggle.focus();
      });

      // El panel es exclusivo de móvil: al pasar a escritorio se cierra.
      const desktop = window.matchMedia("(min-width: 62rem)");
      desktop.addEventListener("change", (event) => {
        if (event.matches && isOpen()) setOpen(false);
      });
    }
  }
</script>
